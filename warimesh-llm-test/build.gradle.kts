// Top-level build file — plugin versions live here so the app module
// stays clean. Matches the stack used by the main WariMesh app
// (Gradle 8.14, AGP 8.11.1, Kotlin 2.2.20, JDK 17).
plugins {
    id("com.android.application") version "8.11.1" apply false
    id("org.jetbrains.kotlin.android") version "2.2.20" apply false
}
