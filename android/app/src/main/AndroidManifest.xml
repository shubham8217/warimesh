<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- BLE permissions. maxSdkVersion=30 on the legacy pair means Android 12+
         devices use the new BLUETOOTH_SCAN/BLUETOOTH_ADVERTISE/BLUETOOTH_CONNECT
         permissions instead; both are declared so the app works on Android 8-11
         and 12+ alike. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- No maxSdkVersion any more. This used to be capped at 30 because the
         only reason the app needed location was Android 11's rule that BLE
         scanning implied it. An SOS now carries the sender's GPS position
         (see LocationPacket in lib/models.dart), so this is needed as a
         real location permission on every version — with the cap left on,
         GPS would have been silently unavailable on Android 12+, which is
         every phone this will realistically run on. Coarse is declared
         alongside it because Android 12+ lets the person grant only
         approximate location, and an approximate position still narrows a
         search from a whole crowd to a corner of it. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- Hardening pass: notification on receipt + foreground service scaffolding -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- On-device LLM inference (MediaPipe tasks-genai) needs no network,
         but the library's native init can touch the storage APIs. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Without a battery-optimization exemption, Android (and aggressively
         so on MIUI/OneUI) freezes the process once the screen goes off —
         which is exactly when an SOS most needs to get through. The app
         asks for this at startup; it's a request, and the person can
         refuse, so the locked-screen path degrades rather than breaks. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Lets an incoming SOS wake the screen instead of sliding past as a
         silent banner. Android 14+ grants this only to calling/alarm-class
         apps by default; if the OS declines, the notification still fires
         as a normal high-priority heads-up. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="WariMesh"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_foreground_task's service. Name must not change.
             foregroundServiceType="connectedDevice" is the Android 14+ category
             for services that talk to an external device over Bluetooth, which
             matches what this service is actually used for (keeping BLE scan and
             advertise alive with the screen off). Whether that goal is actually
             achieved is unverified; see the honesty note in lib/main.dart. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ hides other installed apps unless they're declared
             here. Without these three, launching walking directions to an
             alert's coordinates (see lib/directions.dart) would be blocked
             even with Google Maps installed. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="google.navigation"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
