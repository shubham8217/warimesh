org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Machine-local JDK pin. Gradle 8.14 cannot run on the Android-Studio-
# bundled JDK 25 (Kotlin's JavaVersion parser fails on "25.0.2"), and this
# project's Kotlin targets JDK 17 bytecode. The path below is macOS
# (Homebrew openjdk@17); on other machines set this to a local JDK 17/21/23
# install or override in ~/.gradle/gradle.properties.
org.gradle.java.home=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
