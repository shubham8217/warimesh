org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Pinned to the JDK 23 install on this machine (C:\Program Files\Java\jdk-23),
# project-scoped only -- does not touch global Flutter/Android Studio config.
# Flutter otherwise defaults to the Android-Studio-bundled JDK (25.0.2 here),
# which this project's Gradle 8.14 cannot run ("What went wrong: 25.0.2").
org.gradle.java.home=C\:\\Program Files\\Java\\jdk-23
